import React from 'react'


const Main = ({name, date}) => {
        return (
                <div>
                        <h2>{name}</h2>
                        <p>{date}</p>
                </div>
        )
}

export default Main;